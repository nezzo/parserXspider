 Сначала надо написать в процедурном режиме коде по  парсингу сайтов, а дальше перевести код в паттерн

Заполнять категории нужно с id = 2  так как 1 это каталог в дотпланте 


https://bikeland.ru/

Надо сначала кодом пройтись по категориям, заполнить в базе отдельно каждую категорию и получить ее id, а дальше сделать типа дерево категорий
как в опенкарт то есть ид категории и ид родительской категории. Для этого нужно делать поиск по  и мени или хз как что бы получить предыдущую
категорию и когда только добавили категорию вернуть ее ид и записать в дерево категории.  А дальше по товару, когда дошли до конца списка категорий
и нету такого тега как у имени категорий (сделать проверку товар есть или нет) то переходим по ссылке на товар и сохраняем эту страницу, даем ей  название id категории.html (когда спарсим товар то удаляем файл) и так делае  в категории по пагинации до конца списка. Далее с помощью специального тега получаем название категории, ищем в базе получаем его ид (делаем проверку если есть, если нету то просто сохраняем без категории), этот ид держим в памяти и каждый новый добавленый товар подтягиваем этот ид. После того как допарсили одну страницу то уничтожаем эту переменную которая хранит это ид, открываем новую страницу и таже процедура.

Следующее действие такого:(каждый раз сохраняем файл для парсинга, что бы не нагружать сервак вечным подключением к сайту) Запускаем первую функцию которая парсит родителей, в базе создаем типа кэша по категория где будут идти ссылки на дочерние категории, а рядом будет стоять ид родителя. После того как занесли данные в базу начинаем проходить по ссылкам. Берем ид родителя и каждой добавленной категории прописываем ид родителя. Переходя по ссылкам с базе надо делать проверку на дочерние категории, если такие существуют то берем их ссылки и сохраняем в базе но при этом ставим ид предыдущего родителя (не начального). И так проходим по всем категориям. Главное когда переходим по ссылкам для парса сохранять страницу, что бы админы не пропасли, что их сайт парсят




https://www.drivebike.ru 
Тут почти тоже самое только нужно чутка переделать тут идут разделы и общая категория нужно добавлять товар туда и туда + тут идет текст до категории и подткагории
